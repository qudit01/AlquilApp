<section class="index_user">
<%= turbo_frame_tag :modal do %>
  <div class="modals" data-controller="turbomodals">
    <div class="form_container">
      <div class="title">
        <h1 class="add_edit_title"><%= 'Ingresar dinero' %></h1>
      </div>
      <%= render 'form', method: :patch, url: wallet_path(current_user.wallet) %>
    </div>
  </div>
<% end %>
</section>