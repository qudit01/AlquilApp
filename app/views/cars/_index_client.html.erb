<div class="container">
  <span class="add-car-span">
    <h2>Listado de Automoviles</h2>
  </span>
  <% if current_user.license.state == 'pending' || current_user.license.state == 'expired' || current_user.license.state == 'rejected' %>
     <h1> Por favor cargue una foto de su licencia de conducir valida para poder utilizar la app, si ya lo hizo, por favor verifique que no haya sido rechazada, o bien espere a que un supervisor la verifique a la brevedad. </h1>
  <% else %>
    <% if @cars.length == 0 %>
      <h1> No hay autos disponibles en un radio de 5 cuadras de su ubicación actual, si desea ver todos los autos disponibles presione el botón </h1>
        <td><%= link_to "+ Ver más autos", index_ver_mas_autos_path ,  class: 'btn', spadding: '14px 25px' %></td>
    <% else %>
      <table class="table">
        <tr>
          <thead>
            <th>Imagen </th>
            <th>Numero de auto </th>
            <th>Color </th>
            <th>Patente </th>
            <th>Marca </th>
            <th>Modelo </th>
            <th>Seguro </th>
            <th>Kilometros </th>
          </thead>
        </tr>
      </table>
      <% @cars.each do |c| %>
        <table class="table">
          <tr>
            <td><%= image_tag c.photo.url(:thumb).to_s %></td>
            <td><%= c.car_number %></td>
            <td><%= c.color %></td>
            <td><%= c.plate %></td>
            <td><%= c.brand %></td>
            <td><%= c.model %></td>
            <td><%= c.insurance %></td>
            <td><%= c.kilometers %></td>
        </tr>
        </table>
      <% end %>
    <% end %>
  <% end %>

  <div>
    <img src="https://maps.google.com/maps/api/staticmap?center=Chicago,IL&zoom=8&size=512x512&maptype=roadmap&sensor=false&key=AIzaSyATZsyZ3w93zw2n3bGuZx_kwnHhHRfVQHM" alt="Static Map of La Plata"></img>
  </div>


</div>

